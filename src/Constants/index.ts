import { HEIGHT, WIDTH } from "./dimensions";
import { BORDER_RADIUS, MARGIN, PADDING } from "./size";
import typo from "./Typo";

export { HEIGHT, WIDTH, typo as TYPO, PADDING, MARGIN, BORDER_RADIUS };
